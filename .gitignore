# Dependencies and build output (regenerated by `npm install` / the build) — never commit.
node_modules/
dist/
dist-electron/
release/
.vite/

# TypeScript / tooling caches
*.tsbuildinfo

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
